SASSDOCIFY(1)               General Commands Manual              SASSDOCIFY(1)



NAME
       sassdocify - make a SassDoc branch

SYNOPSIS
       sassdocify [options] [src]

DESCRIPTION
       sassdocify  creates a gh-pages branch in the current git(1) repository,
       and documentize the src directory within it.

       If src is not given, it's set to the current directory.

OPTIONS
       -h, --help
              Show help.

       -V, --version
              Show version.


       -m MSG, --message=MSG
              Show version.

SEE ALSO
       git(1), http://sassdoc.com/



                                                                 SASSDOCIFY(1)
